use crate::sudoku::sudoku::Sudoku;

/// Removes candidates (notes which indicate a given square could be a given number)
/// based on the numbers present in the board.
pub fn prune_neighbors(board: &mut Sudoku) {
    for each in board.grid {}
}

fn identify_containing_column() {}

fn identify_containing_row() {}

fn identify_containing_block() {}
